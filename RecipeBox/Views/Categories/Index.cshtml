@{
  Layout = "_Layout";
}

@using RecipeBox.Models;

@if (@Model.Count == 0)
{
  <p>No hay nada guey</p>
}

@foreach (Category category in Model)
{
  <li>@Html.ActionLink($"{category.Name}", "Details", new { id = category.CategoryId })</li>
}

<p>@Html.ActionLink("Create Category", "Create")</p>