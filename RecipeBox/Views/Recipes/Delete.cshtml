@{
  Layout = "_Layout";
}

@model RecipeBox.Models.Recipe

<p>Are you sure you want to delete this Recipe entry from the database?</p>

<p><strong>@Html.DisplayNameFor(targetRecipe => targetRecipe.Title)</strong>: @Html.DisplayFor(targetRecipe => targetRecipe.Title)</p>

@using(Html.BeginForm())
{
  <input type="submit" value="Delete Recipe"/>
}

<hr/>
@Html.ActionLink("Back to Recipe Details", "Details", new { id = Model.RecipeId })